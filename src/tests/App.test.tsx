import React from "react";
import { render } from "@testing-library/react";
import App from "../App";

describe("<App /> tests", () => {
  it("renders <App />", () => {
    render(<App />);
  });
});
